apiVersion: v1
kind: Service
metadata:
  name: springboot-crud-v2
  namespace: v2
spec:
  selector:
    app: springboot-crud
    version: v2
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
