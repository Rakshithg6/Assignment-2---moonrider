apiVersion: v1
kind: Service
metadata:
  name: springboot-crud-v1
  namespace: v1
spec:
  selector:
    app: springboot-crud
    version: v1
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
